# Azure Pipelines Agent Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it contains secrets!

# Azure DevOps Organization URL
# Example: https://dev.azure.com/yourorganization
AZP_URL=https://dev.azure.com/YOURORGANIZATION

# Personal Access Token with Agent Pools (read, manage) permissions
# Generate at: https://dev.azure.com/YOURORGANIZATION/_usersSettings/tokens
# KEEP THIS SECRET - never commit the actual token
AZP_TOKEN=your_personal_access_token_here

# Agent Pool Name (create in Project Settings > Agent pools)
# Use "Default" if you haven't created a custom pool
AZP_POOL=Default

# Agent Name (optional - will auto-generate if not provided)
# Make this unique to avoid conflicts
AZP_AGENT_NAME=my-docker-agent

# Optional: Custom work directory
# AZP_WORK=/home/azp/_work

# Optional: Specific agent version
# AZP_AGENT_VERSION=3.232.3